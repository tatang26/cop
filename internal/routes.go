package internal

import (
	"github.com/leapkit/core/server"
)

func SetRoutes(r server.Router) {
	// Hey there! Don't forget to add the render engine here!

	// Also mapping your handlers here! :D

	// ...the assets should be included here too ;)
}
